# qwen38 生态 + Hermes qwen-思考 接交接

> 说「**按交接提示继续**」→ 新会话读 `内存 qwen38-clone-switch-resume-handoff` + 本文 + `archive-20260823-v2.md`，即可无缝续接。

## 一、项目现状（2026-08-23）
qwen38-27B 自建部署生态：**4 台 AutoDL 服务器** + 本机 Hermes/codex 接入。用户主用 **qwen 带思考做代码开发（Hermes）**。

## 二、4 台服务器
| 实例 | ssh 别名 | 卡/sm | codex | Hermes | 思考 |
|---|---|---|---|---|---|
| 5090生产 | autodl-5090 | 5090/sm120 | codex-q38/-free | — | off(离线) |
| vGPU | autodl-qwen-vgpu | 4080/sm89 | codex-q38new-free(隧道6112) | — | off(离线) |
| clone1 | autodl-qwen-clone1 | 4080SUPER/sm89 | codex-clone1-free | — | on(离线) |
| clone2 | autodl-qwen-clone2 | 4080/sm89 | （被思考占用） | **qwenthink** | **on 无budget(在线)** |

服务端口 6006；262K 全功能档；6 key；build(sm120)/build89(sm89) 分目录。

## 三、用法速览
- **qwen 带思考开发（主用）**：新开 PowerShell → `hermes-qwen` → 进 qwen3.8-27b 带思考会话（显示思考过程）。原理：per-window profile `qwenthink`＝clone2。
- **codex（可选，需关思考的机）**：`codex-clone2-free` 等（但 clone2 现在是思考档，会导致连不上；要 codex 用 clone2 需改回 off）。
- **服务器**：`ssh autodl-qwen-clone2` 起/看服务（`bash /root/autodl-tmp/start_clone89.sh`）。

## 四、关键约束/坑
1. **codex 与服务器思考互斥**：codex 走 /v1/responses，服务器--reasoning on 时 responses 超时→codex "high demand"。codex 用必须机上 `--reasoning off`；codex CLI 强制 high effort 关不掉。
2. **思考成本**：开思考=~26倍慢/42倍token（完整响应实测）；`--reasoning-budget` 只对 chat 有效。budget 甜区 500。
3. 5090 生产全链路不动；服务器按小时计费用完关。
4. `hermes-switch`/`hermes-qwen` 已改安全锚定版（只动 model 块，别回退旧逻辑）。

## 五、续接路径
- 记忆：`qwen38-clone-switch-resume-handoff`（入口）+ `qwen38-clone-switch-20260823`（全量）
- 文档：`docs/archive-20260823-v2.md`、`docs/benchmarks/*`（压测）、`docs/DFLASH2_research.md`
- 独立项目：`qwen38-vgpu-deploy`｜ skill：`qwen38-clone-server`

## 六、待办
- 用 clone2 思考则 codex-clone2-free 不可用（二选一）；要提速给 clone2 加 --reasoning-budget。
- 5090/vGPU/clone1 离线，要用须控制台开机。