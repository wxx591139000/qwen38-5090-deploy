# 归档摘要 2026-08-20

## 归档信息
- **标签**：`archive-20260820`
- **日期**：2026-08-20
- **仓库**：`wxx591139000/qwen38-5090-deploy`
- **说明**：Qwen3.8-27B 单卡 RTX 5090 生产部署 + VS Code ACP 接入全链路完成并验证

> **完整归档 v2**：`archive-20260820-v2`（2026-08-20）——追加朋友版 SOP、key 分发记录、
> 技能配置恢复等收尾内容，归档状态冻结。

## 本次归档变更摘要

### 部署完成（AutoDL RTX 5090 32GB）
1. **模型**：官方 `ggml-org/Qwen3.8-27B-GGUF` Q4_K_M（18.97GB）+ MTP 头（1.68GB）+ mmproj 视觉投影（0.63GB），ModelScope 16 线程并行下载，字节数全部校验
2. **编译**：llama.cpp 最新 master sm_120 无卡编译（-j3 封顶；97% 时 server-task OOM 后 -j2 续跑）
3. **模板**：从 GGUF 提取并修复——system 位置容忍 + 多轮空 thinking 块（qwen3.6「无法连续推理」根因修复）
4. **服务**：默认 **262K 全功能档**（MTP 推测解码 + mmproj 图片输入，显存 27.7GB，实测通过）
5. **512K 档**：YaRN 扩长 + 自定义补丁（移除 llama-server 槽位上下文 262K 封顶）+ mmproj，显存 30.4GB，实测通过（无 MTP）
6. **公网**：AutoDL 自定义服务 6006 → `https://u1068217-x891-c4947d56.westd.seetacloud.com:8443`

### 本地对接（VS Code ACP，连续推理打通）
7. `~/.codex/config.toml` 新增 `[model_providers.qwen38]`（wire_api=responses）
8. VS Code `settings.json` → `acp.agents` 新增「Codex CLI (Qwen38)」
9. `~/.codex/models.json` 注册 `qwen3.8-27b`（context 262144，消除 metadata fallback 警告）
10. PowerShell profile 新增 `codex-q38` / `codex-q38-free` 别名
11. 验证全通过：chat 多轮跨轮记忆、Responses API、codex exec 多步代理循环、
    ACP 协议同会话三轮连续对话（thoughtTokens 0）、图片输入 chat/responses 双通道

### 上下文配置演进
12. 131K → 512K（YaRN + 补丁，MTP 会翻倍 KV 导致 OOM）→ 定稿 **262K 全功能档为默认**、
    512K+视觉档备选；1M 经测算 32GB 卡不可行（>38GB）

### 朋友接入（完整归档 v2 新增）
13. `docs/FRIEND_SOP.md`：朋友版使用 SOP（三要素 + 自测 + Python/图片示例 + 使用须知 + 排障），
    key 用占位符，不入库
14. key 分发：6 把（主 key + user1~5），朋友 D 已分配 user4 专属 key 并随 SOP 交付；
    其余朋友按相同模板分发

## 当前部署状态
- **服务端**：RTX 5090 运行 llama-server（262K + MTP + mmproj），公网可用，6 把 key
- **本地**：`codex-q38` 命令可用；VS Code ACP 面板可选「Codex CLI (Qwen38)」
- **朋友**：SOP 就绪，user4 已交付，其余待发
- **待办**：高并发压测、512K 档按需切换（一条命令 + models.json 改 524288）、
  朋友用量观察/限流（人多则上 LiteLLM 网关）

## Git 提交统计
- 提交：12 个（初始化 → 部署 → 验证 → 上下文升级 → 归档 → SOP）
- 标签：`archive-20260820`、`archive-20260820-v2`（完整归档）
